{
  "name": "homepage-app",
  "version": "0.0.0",
  "description": "",
  "author": "",
  "scripts": {
    "start": "npm --prefix server start",
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build",
    "build:development": "cd client && npm install && npm run build:development && cd ../server && npm install && npm run build",
    "watch": "cd client && npm run watch",
    "server": "npm --prefix server run dev",
    "front": "cd client && ng serve",
    "prettier": "prettier --write ."
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.3.2"
  }
}
